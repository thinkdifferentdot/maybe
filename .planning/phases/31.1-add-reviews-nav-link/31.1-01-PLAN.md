---
phase: 31.1-add-reviews-nav-link
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/views/transactions/index.html.erb, config/locales/views/transactions/en.yml]
autonomous: true
---

<objective>
Add navigation link to reviews page on transactions index.

Purpose: Users need a way to access the review screen (/transactions/reviews) created in Phase 31. Currently, the reviews page exists but has no navigation link for users to discover it.

Output: A new "Review AI suggestions" menu item in the transactions page dropdown menu that links to reviews_path.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 31 created the reviews page
@.planning/phases/31-feedback-ui/31-01-SUMMARY.md

# Existing menu structure on transactions index
@app/views/transactions/index.html.erb

# Menu component API
@app/components/DS/menu.rb

# Reviews route already exists
@config/routes.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reviews link to transactions menu</name>
  <files>app/views/transactions/index.html.erb</files>
  <action>Add a new menu item to the DS::Menu dropdown that links to reviews_path. The item should:
  - Use variant: "link" like other navigation items
  - Have an appropriate icon (use "clipboard-check" or "check-circle" for review/feedback concept)
  - Set text to t(".review_ai") which will be "Review AI suggestions"
  - Navigate to reviews_path (already routes to Transactions::ReviewsController#index)
  - Place it after "Edit imports" and before "Import" in the menu item order

  The new menu item line should follow the existing pattern:
    <% menu.with_item(variant: "link", text: "Review AI suggestions", href: reviews_path, icon: "clipboard-check", data: { turbo_frame: :_top }) %>
  </action>
  <verify>Visit /transactions, click the menu dropdown, verify "Review AI suggestions" item appears and navigates to /transactions/reviews</verify>
  <done>Menu item visible in dropdown, clicking it navigates to reviews page</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n key for reviews link</name>
  <files>config/locales/views/transactions/en.yml</files>
  <action>Add the "review_ai" translation key under the transactions.index section. The value should be "Review AI suggestions" to match the text used in Phase 31's reviews page header.

  Add this line to the en.yml file under transactions.index:
    review_ai: "Review AI suggestions"

  Then update the menu item in index.html.erb to use t(".review_ai") instead of hardcoded text.
  </action>
  <verify>grep review_ai config/locales/views/transactions/en.yml returns the translation key</verify>
  <done>Translation key exists, menu uses i18n helper</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Menu item appears in transactions page dropdown
- [ ] Clicking menu item navigates to /transactions/reviews
- [ ] i18n key exists and is used (no hardcoded English text)
- [ ] No errors in browser console on page load
</verification>

<success_criteria>

- Reviews link accessible from transactions index menu
- Link properly localized
- Reviews page loads without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/31.1-add-reviews-nav-link/31.1-01-SUMMARY.md`
</output>
